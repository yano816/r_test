<%= form_with(model: diary, local: true) do |form| %>
  <% if diary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(diary.errors.count, "error") %> prohibited this diary from being saved:</h2>

      <ul>
      <% diary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :名前 %>
    <%= form.text_field :name, id: :diary_name %>
  </div>

  <div class="field">
    <%= form.label :ひとこと %>
    <%= form.text_area :title, id: :diary_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
